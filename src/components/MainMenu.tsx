import { useState } from "react";
import {
    View,
    Text,
    Button,
    Image,
} from "react-native"

const MainMenu = () => {
    return (
        <View>
            <Button onPress={() => {}} title=""/>
            <Text> Hello </Text>
        </View>
    );
};

export default MainMenu;
